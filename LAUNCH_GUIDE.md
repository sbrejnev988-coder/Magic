# 🚀 Гайд по запуску MysticBot (@Mystictestuserbot)

## 📦 Требования

- **Windows 11** (или 10)
- **Python 3.10+** (рекомендуется 3.11, 3.12)
- **Telegram-аккаунт** (для тестирования)
- **Токен бота** (уже есть: `8270359511:AAG4UZjVxzlf6LPC9YaqNzZyf20aShEM1iA`)

## 🛠️ Шаг 1: Подготовка окружения

### 1.1. Проверка Python
Открой PowerShell (администратор не нужен) и выполни:
```powershell
python --version
```
Должно быть `Python 3.10` или выше. Если Python не установлен, скачай с [python.org](https://python.org) (отметь галочку "Add Python to PATH").

### 1.2. Клонирование проекта (если нужно)
Проект уже находится в `C:\Users\Kekl\.openclaw\workspace\MysticBot`.  
Если нужно скопировать в другое место:
```powershell
cd C:\Users\Kekl\Desktop
Copy-Item -Recurse "C:\Users\Kekl\.openclaw\workspace\MysticBot" .
```

## 📦 Шаг 2: Установка зависимостей

### 2.1. Переход в папку проекта
```powershell
cd "C:\Users\Kekl\.openclaw\workspace\MysticBot"
```

### 2.2. Установка пакетов
```powershell
pip install -r requirements.txt
```
Если `pip` не найден, используй `python -m pip`.

**Основные зависимости:**
- `aiogram==3.3.0` – фреймворк для Telegram ботов
- `sqlalchemy>=2.0` – работа с базой данных
- `aiosqlite` – асинхронный SQLite
- `httpx` – HTTP-клиент для запросов к API
- `python-dotenv` – загрузка переменных окружения

## ⚙️ Шаг 3: Настройка конфигурации

### 3.1. Файл `.env`
В папке `MysticBot` уже есть файл `.env` с настройками:
```
BOT_TOKEN=8270359511:AAG4UZjVxzlf6LPC9YaqNzZyf20aShEM1iA
DATABASE_URL=sqlite:///./bot/database/mysticbot.db
LOG_LEVEL=INFO
RATE_LIMIT=3
RATE_WINDOW=10

# Модули (все включены)
ENABLE_TAROT=true
ENABLE_NUMEROLOGY=true
ENABLE_HOROSCOPE=true
ENABLE_FINANCE_CALENDAR=true
ENABLE_DREAM=true
ENABLE_RUNES=true
ENABLE_RANDOM=true
ENABLE_ASK=true
ENABLE_ASTROLOGY=true
ENABLE_MEDITATION=true
```

### 3.2. Проверка настроек
Если нужно изменить БД на PostgreSQL или добавить Redis – отредактируй `.env`.

## 🚀 Шаг 4: Запуск ботов

### 4.1. Архитектура ботов
Теперь MysticBot состоит из **двух независимых ботов**:
1. **Основной бот** (`bot/main.py`) – для пользователей, со всеми эзотерическими функциями
2. **Бот-консультант** (`admin_bot.py`) – «коморка» для консультанта, просмотр истории, чеков, черновиков

**Токены ботов:**
- Основной: `8270359511:AAG4UZjVxzlf6LPC9YaqNzZyf20aShEM1iA` (@Mystictestuserbot)
- Консультант: `8456022927:AAE-GyMRdyHbxFNxYFtnR78GTPH8aiSNTqc` (отдельный бот)

### 4.2. Запуск всех ботов одной командой (рекомендуется)
Самый простой способ — использовать скрипт `run_bots.py`:

```powershell
cd "C:\Users\Kekl\.openclaw\workspace\MysticBot"
python run_bots.py
```

**Ожидаемый вывод:**
```
2026-02-14 09:20:00 | INFO | bots_launcher | ЗАПУСК ВСЕХ БОТОВ MYSTICBOT
2026-02-14 09:20:00 | INFO | bots_launcher | Запускаю Основной бот (bot/main.py)...
2026-02-14 09:20:02 | INFO | bots_launcher | Основной бот запущен (PID: 12345). Логи в logs/main_bot.log
2026-02-14 09:20:02 | INFO | bots_launcher | Запускаю Бот-консультант (admin_bot.py)...
2026-02-14 09:20:04 | INFO | bots_launcher | Бот-консультант запущен (PID: 12346). Логи в logs/consultant_bot.log
2026-02-14 09:20:04 | INFO | bots_launcher | Успешно запущено 2/2 ботов.
```

**Дополнительные параметры:**
```powershell
# Запуск в фоновом режиме (демон)
python run_bots.py --daemon

# Остановка всех запущенных ботов
python run_bots.py --stop

# Асинхронный запуск (экспериментально)
python run_bots.py --async
```

### 4.3. Запуск ботов по отдельности

**Основной бот:**
```powershell
cd "C:\Users\Kekl\.openclaw\workspace\MysticBot"
python -m bot.main
```

**Бот-консультант:**
```powershell
cd "C:\Users\Kekl\.openclaw\workspace\MysticBot"
python admin_bot.py
```

### 4.4. Запуск в фоне (как служба)
```powershell
# Запуск всех ботов в фоне
cd "C:\Users\Kekl\.openclaw\workspace\MysticBot"
python run_bots.py --daemon

# Или по отдельности
Start-Process python -ArgumentList "-m bot.main" -WindowStyle Hidden
Start-Process python -ArgumentList "admin_bot.py" -WindowStyle Hidden
```

### 4.5. Остановка ботов
**Если боты запущены через `run_bots.py`:** нажмите `Ctrl+C` в консоли.

**Остановка всех ботов:**
```powershell
cd "C:\Users\Kekl\.openclaw\workspace\MysticBot"
python run_bots.py --stop

# Или вручную через PowerShell
Get-Process python | Where-Object { 
    $_.Path -like "*MysticBot*" -or 
    $_.CommandLine -like "*bot/main*" -or 
    $_.CommandLine -like "*admin_bot*" 
} | Stop-Process
```

## 🧪 Шаг 5: Тестирование

### 5.1. Основной бот (@Mystictestuserbot)
1. **Открой Telegram** (Desktop, Web или мобильное приложение)
2. **Найди бота:** @Mystictestuserbot
3. **Отправь команды:**
   - `/start` – главное меню
   - `/help` – справка
   - `/ask "Что значит карта Шут?"` – консультация с AI
   - `/history` – история консультаций (после использования `/ask`)

### 5.2. Бот-консультант
**Важно!** Бот-консультант — это отдельный бот со своим токеном. Его нужно добавить в Telegram отдельно.

1. **Добавление бота-консультанта:**
   - В Telegram найдите `@BotFather`
   - Отправьте `/newbot` и следуйте инструкциям
   - **ИЛИ** используйте готовый токен (если бот уже создан):
     - Перейдите по ссылке: `https://t.me/MysticConsultantBot` (если создан)
     - Или найдите по токену (нужен поиск через BotFather)

2. **Команды бота-консультанта:**
   - `/consultant` – меню консультанта
   - `/consultations` – просмотр истории консультаций
   - `/orders` – управление заказами
   - `/drafts` – черновики на проверку (гибридный режим)
   - `/stats` – статистика

3. **Интеграция с гибридным режимом:**
   - Пользователь задаёт вопрос в гибридном режиме
   - Основной бот сохраняет черновик в БД
   - **Автоматически отправляет уведомление** консультанту в личку
   - Консультант заходит в своего бота → «Черновики» → проверяет/редактирует

### 5.3. Тестирование гибридного режима с консультантом
1. **Запустите всех ботов** (`python run_bots.py`)
2. **В основном боте:** нажмите «🔄 Гибридный режим»
3. **Задайте любой вопрос** – получите черновик
4. **Как консультант:** откройте бота-консультанта → `/drafts`
5. **Проверьте черновик** – одобрите, отредактируйте или отклоните
6. **Пользователь получит ответ** после проверки консультантом

### 5.4. Если бот не отвечает:
- Проверь, запущен ли процесс (Диспетчер задач → Python)
- Проверь логи в папке `logs/`
- Убедись, что бот не заблокирован в Telegram
- Проверь `.env` файл (токены должны быть правильными)

## 🗃️ Шаг 6: Структура проекта

```
MysticBot/
├── bot/                          # Основной бот
│   ├── main.py                   # Точка входа основного бота
│   ├── config.py                 # Конфигурация
│   ├── handlers/                 # Обработчики команд
│   │   ├── ai_mode.py            # Режим ИИ и гибридный режим
│   │   ├── ask.py                # Консультации AI
│   │   ├── consultant.py         # Роутер консультанта (для admin_bot.py)
│   │   ├── admin.py              # Админ-панель
│   │   └── ...                   # Другие обработчики
│   ├── services/                 # Бизнес-логика
│   ├── models/                   # Модели БД
│   ├── database/                 # Подключение к БД
│   ├── middlewares/              # Промежуточное ПО
│   └── prompts/                  # Промпты для AI
├── admin_bot.py                  # Бот-консультант (отдельный бот)
├── run_bots.py                   # Скрипт запуска всех ботов
├── logs/                         # Логи ботов (создаётся автоматически)
├── .env                          # Переменные окружения
├── requirements.txt              # Зависимости
├── LAUNCH_GUIDE.md               # Этот гайд
└── README.md                     # Описание проекта
```

## 🔄 Шаг 7: Автоматический перезапуск (опционально)

### 7.1. Через планировщик Windows (для всех ботов)
1. Открой **Планировщик заданий** (Task Scheduler)
2. Создай задачу:
   - Триггер: "При запуске системы"
   - Действие: "Запустить программу"
   - Программа: `python`
   - Аргументы: `run_bots.py --daemon`
   - Рабочая папка: `C:\Users\Kekl\.openclaw\workspace\MysticBot`

### 7.2. Через скрипт-обёртку
**Для всех ботов** создай `start_all_bots.bat`:
```batch
@echo off
cd /d "C:\Users\Kekl\.openclaw\workspace\MysticBot"
python run_bots.py
pause
```

**Для основного бота** (устаревший способ):
```batch
@echo off
cd /d "C:\Users\Kekl\.openclaw\workspace\MysticBot"
python -m bot.main
pause
```

### 7.3. Как служба Windows (продвинутый вариант)
1. Создай файл `mysticbot_service.py`:
```python
import subprocess
import time
import sys

while True:
    try:
        proc = subprocess.Popen([sys.executable, "run_bots.py", "--daemon"])
        proc.wait()
    except KeyboardInterrupt:
        break
    except Exception as e:
        print(f"Ошибка: {e}")
    time.sleep(5)
```

2. Создай службу Windows через `nssm` (Non-Sucking Service Manager):
```powershell
# Скачать nssm: https://nssm.cc/download
nssm install MysticBot python "C:\Users\Kekl\.openclaw\workspace\MysticBot\run_bots.py" --daemon
nssm start MysticBot
```

## 🛠️ Шаг 8: Решение проблем

### ❌ Ошибка "Conflict: terminated by other getUpdates request"
**Причина:** Запущено несколько экземпляров бота.  
**Решение:**
```powershell
# Убить все процессы Python
Get-Process python | Stop-Process -Force

# Удалить вебхук
$token = "8270359511:AAG4UZjVxzlf6LPC9YaqNzZyf20aShEM1iA"
Invoke-RestMethod -Uri "https://api.telegram.org/bot$token/deleteWebhook"

# Запустить бота заново
cd "C:\Users\Kekl\.openclaw\workspace\MysticBot"
python -m bot.main
```

### ❌ "ModuleNotFoundError: No module named 'aiogram'"
**Решение:** Установи зависимости:
```powershell
pip install aiogram sqlalchemy aiosqlite httpx python-dotenv
```

### ❌ Бот не отвечает на команды
1. Проверь, что сообщение отправлено именно боту (@Mystictestuserbot)
2. Проверь логи бота на ошибки
3. Попробуй перезапустить бота

### ❌ Проблемы с ботом-консультантом
**1. Бот-консультант не запускается:**
- Проверьте токен в `.env` (должен быть `ADMIN_BOT_TOKEN=8456022927:...`)
- Убедитесь, что бот создан в @BotFather
- Проверьте логи: `logs/consultant_bot.log`

**2. Консультант не получает уведомления о черновиках:**
- Убедитесь, что `ADMIN_USER_ID` в `.env` правильный (ваш Telegram ID)
- Проверьте, что гибридный режим работает (пользователь получает черновик)
- Проверьте таблицу `hybrid_drafts` в БД

**3. Конфликт двух ботов:**
Если оба бота используют один и тот же токен (ошибка):
```powershell
# Остановите все боты
python run_bots.py --stop

# Проверьте .env файл, убедитесь что:
# BOT_TOKEN=8270359511:... (основной бот)
# ADMIN_BOT_TOKEN=8456022927:... (бот-консультант)

# Запустите заново
python run_bots.py
```

### ❌ Ошибки базы данных
**Миграции не выполнены:**
```powershell
# Удалите старую БД (если нужно)
Remove-Item "bot/database/mysticbot.db" -Force

# Запустите бота - таблицы создадутся автоматически
python run_bots.py
```

**Нет таблицы hybrid_drafts:**
Бот создаст её автоматически при первом запуске. Если нет — проверьте права на запись в папку `bot/database/`.

## 📊 Шаг 9: Мониторинг

### Логи
Логи выводятся в консоль и включают:
- Запуск модулей
- Запросы пользователей
- Ошибки (если есть)

### База данных
База SQLite: `bot/database/mysticbot.db`  
Можно открыть в [DB Browser for SQLite](https://sqlitebrowser.org/).

### Статистика
Бот ведёт историю консультаций в таблице `consultations`.

## 🧹 Авто‑компакт контекста OpenClaw

**OpenClaw автоматически** делает compact контекста, когда он превышает ~115к токенов.  
**Вручную** можно вызвать компакт через команду `/compact` в Telegram.

### Что остаётся после компакта:
- `SOUL.md`, `AGENTS.md`, `app.js`, `README.md`
- Последний проект (отсортированный по изменению)
- `memory/YYYY-MM-DD.md` (если есть)

## 🚀 Быстрый старт (краткая версия)

1. **Открой PowerShell**
2. **Перейди в папку проекта:**
   ```powershell
   cd "C:\Users\Kekl\.openclaw\workspace\MysticBot"
   ```
3. **Запусти всех ботов:**
   ```powershell
   python run_bots.py
   ```
4. **Открой Telegram:**
   - Основной бот: @Mystictestuserbot → `/start`
   - Бот-консультант: добавьте бота по токену `8456022927:...` → `/consultant`

**Альтернатива** (только основной бот):
```powershell
python -m bot.main
```

## 📞 Поддержка

Если возникли проблемы:
1. Проверь логи бота
2. Посмотри `memory/2026-02-14.md` (история разработки)
3. Напиши @openclawmaximbot

---

**Удачи! Бот готов к работе.**  
*MysticBot — твой эзотерический помощник.*