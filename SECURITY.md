# Руководство по безопасности MysticBot

## ⚠️ Критические правила

### 1. НЕ коммитьте `.env` файл в репозиторий

**НИКОГДА не добавляйте `.env` файл в Git!**

Файл `.env` уже добавлен в `.gitignore`. Если вы случайно закоммитили его:

```bash
# Удалите из Git (но сохраните локально)
git rm --cached .env
git commit -m "security: Remove .env file"
git push

# ОБЯЗАТЕЛЬНО: смените ВСЕ API-ключи и токены!
```

### 2. Защита API-ключей

#### Локальная разработка:
1. Скопируйте `.env.example` в `.env`:
   ```bash
   cp .env.example .env
   ```

2. Заполните свои ключи в `.env`

3. Убедитесь, что `.env` в `.gitignore`:
   ```bash
   grep -q "^\.env$" .gitignore && echo "OK" || echo "Add .env to .gitignore!"
   ```

#### Production развертывание:

**Рекомендуемые методы (в порядке приоритета):**

1. **Secrets Management System**
   - HashiCorp Vault
   - AWS Secrets Manager
   - Azure Key Vault
   - Google Cloud Secret Manager

2. **Переменные окружения Docker/Kubernetes**
   ```yaml
   # docker-compose.yml (пример)
   services:
     mysticbot:
       environment:
         - BOT_TOKEN=${BOT_TOKEN}
         - FEATHERLESS_API_KEY=${FEATHERLESS_API_KEY}
       env_file:
         - /secure/path/.env  # вне репозитория
   ```

3. **CI/CD Secrets**
   - GitHub Secrets
   - GitLab CI/CD Variables
   - Jenkins Credentials

### 3. Безопасные права доступа к файлам

```bash
# Ограничьте доступ к .env
chmod 600 .env

# Проверьте владельца
ls -la .env
# Должно быть: -rw------- 1 user user
```

### 4. Ротация ключей

Регулярно меняйте API-ключи:
- **Telegram Bot Token**: каждые 3-6 месяцев
- **LLM API Keys**: каждые 1-3 месяца
- **Database passwords**: каждые 6 месяцев

При компрометации — **немедленно** смените все ключи!

---

## 🔒 Production рекомендации

### База данных

1. **Используйте PostgreSQL вместо SQLite**
   ```env
   DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/mysticbot
   ```

2. **Шифрование соединения**
   - SSL/TLS для базы данных
   - Сертификаты для проверки сервера

3. **Регулярные бэкапы**
   ```bash
   # Пример бэкапа PostgreSQL
   pg_dump -h localhost -U user mysticbot > backup_$(date +%Y%m%d).sql
   ```

### Redis

1. **Аутентификация**
   ```env
   REDIS_URL=redis://:password@localhost:6379/0
   ```

2. **TLS для Redis**
   ```env
   REDIS_URL=rediss://:password@localhost:6380/0
   ```

### Сетевая безопасность

1. **Firewall правила**
   ```bash
   # Разрешите только необходимые порты
   ufw allow 443/tcp   # HTTPS
   ufw allow 22/tcp    # SSH (только с доверенных IP)
   ufw deny 5432/tcp   # PostgreSQL (закрыт извне)
   ufw deny 6379/tcp   # Redis (закрыт извне)
   ufw enable
   ```

2. **Fail2Ban для SSH**
   ```bash
   apt install fail2ban
   systemctl enable fail2ban
   ```

### Мониторинг

1. **Логирование**
   - Централизованные логи (ELK Stack, Grafana Loki)
   - Ротация логов

2. **Alerts**
   - Мониторинг аномальной активности
   - Уведомления при ошибках

---

## 🐛 Сообщение об уязвимостях

Если вы обнаружили уязвимость в безопасности:

1. **НЕ создавайте публичный Issue** с подробностями
2. Свяжитесь с разработчиком личным сообщением
3. Предоставьте:
   - Описание проблемы
   - Шаги для воспроизведения
   - Возможное решение (если есть)

---

## ✅ Чек-лист безопасности

Перед production деплоем:

- [ ] `.env` файл в `.gitignore`
- [ ] `.env` НЕ в репозитории
- [ ] Уникальные сильные пароли для всех сервисов
- [ ] PostgreSQL вместо SQLite
- [ ] SSL/TLS для всех соединений
- [ ] Firewall настроен
- [ ] Автоматические бэкапы настроены
- [ ] Мониторинг и alerts настроены
- [ ] Rate limiting настроен
- [ ] Логи централизованы
- [ ] Обновления зависимостей проверены
- [ ] Закрепленные версии в requirements.txt

---

## 📚 Дополнительные ресурсы

- [OWASP Top 10](https://owasp.org/www-project-top-ten/)
- [Python Security Best Practices](https://python.readthedocs.io/en/stable/library/security_warnings.html)
- [Docker Security](https://docs.docker.com/engine/security/)
- [Telegram Bot Security](https://core.telegram.org/bots/faq#security)

---

**Последнее обновление**: 15 февраля 2026
