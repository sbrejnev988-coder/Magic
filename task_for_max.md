# Задание для Макса: MysticBot улучшения

**Дата:** 2026-02-14 (обновлено 23:00)  
**Статус:** **Интеграция A1/A2 завершена** (боты запущены, но конфликт getUpdates требует решения)  
**Приоритет:** Высокий  

---

## 📊 Текущее состояние проекта

### ✅ Что уже сделано
1. **Безопасность GitHub токена**
   - Токен сохранён локально в `github_token.txt`
   - Добавлен в `.gitignore`, не попадает в репозиторий
   - Репозиторий `sbrejnev988-coder/Magic` обновлён

2. **Инфраструктура улучшений**
   - **A1 (Персонализация LLM):** создан `bot/models/user_profile.py` (модель профиля пользователя)
   - **A2 (Специализированные промпты):** создан `bot/services/prompt_manager.py` + 11 файлов промптов в `bot/prompts/`
   - **UX‑пакет (Perplexity Research):** созданы все 10 файлов:
     - `bot/callbacks/menu.py`, `bot/keyboards/menu.py`, `bot/keyboards/rating.py`
     - `bot/states/ux.py`, `bot/services/progress.py`, `bot/models/feedback.py`
     - `bot/services/feedback_service.py`, `bot/services/bot_commands.py`
     - `bot/handlers/ux_menu.py` (полный роутер)
     - `migrate_add_feedback_ratings.py` (миграция для SQLite)

3. **Интеграция в основного бота**
   - UX‑роутер добавлен в `bot/main.py`
   - Команды бота настроены через `setup_bot_commands`
   - Миграция таблицы `feedback_ratings` выполнена

4. **Исправление критических ошибок**
   - Модифицирован `bot/database/engine.py`: добавлена поддержка глобального session maker (устраняет `TypeError: get_session_maker() missing 1 required positional argument: 'engine'`)
   - Устранён конфликт `TelegramConflictError` (запущен только один экземпляр бота, PID 32568)

### ⚠️ Критическая проблема: конфликт getUpdates и необработка сообщений
**Суть:** Бот запускается, но не обрабатывает команды (`/menu`, `test`). Периодически возникает конфликт `TelegramConflictError`, указывающий на наличие другого экземпляра бота, который забирает updates.

**Диагностика:**
- Убиты все процессы python (`taskkill /F /IM python.exe`).
- Закомментированы все роутеры кроме `ux_menu_router`, отключены middleware.
- Логи показывают `Update id=... is not handled` или отсутствие обработки.
- Очередь updates часто пуста, что свидетельствует о подтверждении updates другим экземпляром.

**Гипотезы:**
1. **Скрытый процесс** – другой экземпляр бота (админский или старый) работает в фоне.
2. **Планировщик задач** – скрипт запускается по расписанию.
3. **Другой бот `@MysticTestKeklBot`** – возможно, тестовый бот с другим токеном перехватывает updates.
4. **Вирус/майнер** – маловероятно.

**Решение необходимо перед продолжением работы.**

---

## 🎯 Задачи для реализации

### 🚀 Высокий приоритет (сначала это)

#### 1. Устранение конфликта getUpdates и запуск работающего бота
**Цель:** Обеспечить, чтобы основной бот был единственным экземпляром, получающим updates, и обрабатывал команды.

**Действия:**
- [x] **Временное переименование `admin_bot.py`** – предотвратить его возможный запуск.
- [ ] **Идентификация `@MysticTestKeklBot`** – найти процесс, токен, источник.
- [ ] **Остановка всех процессов Python** – убить скрытые экземпляры ботов.
- [ ] **Перезагрузка компьютера** – гарантированно убить все скрытые процессы.
- [ ] **Проверка планировщика задач** – найти задачи, запускающие Python‑скрипты, и отключить их.
- [ ] **Поиск скрытых процессов** – через `tasklist /V`, `Get-WmiObject`, проверка служб и автозагрузки.
- [ ] **Запуск бота на другом токене** (опционально) – создать нового бота в @BotFather для тестов.
- [ ] **Восстановление middleware и роутеров** после успешного запуска.

**Ожидаемый результат:** Бот отвечает на `/menu` интерактивной клавиатурой с группировкой по темам, конфликты отсутствуют.

#### 2. Интеграция A1 – Персонализация LLM
**Цель:** Модифицировать `bot/services/llm.py` для использования профиля пользователя при генерации ответов.

**Действия:**
- [x] Создать сервис `profile_service.py` для управления `UserProfile` (получение/обновление)
- [x] Модифицировать `LLMService.generate()`: добавлять в промпт информацию из профиля (предпочтения, история, стиль)
- [x] Добавить вызов `prompt_manager.get_personalized_prompt()` для персонализации системного промпт
- [x] Обновить хэндлеры, использующие LLM (`ai_mode.py`, `ask.py`), чтобы передавать `user_id` и использовать `generate_personalized`

**Ожидаемый результат:** Ответы AI становятся более персонализированными, учитывают историю взаимодействия.

**Статус:** Интеграция завершена. Хэндлеры модифицированы, используют `ProfileService` и `generate_personalized`. Готово к тестированию после решения конфликта getUpdates.

#### 3. Интеграция A2 – Специализированные промпты для 11 модулей
**Цель:** Настроить `PromptManager` для загрузки промптов из `bot/prompts/` и использования их в соответствующих модулях.

**Действия:**
- [x] Реализовать в `PromptManager` методы:
  - `get_module_prompt(module_name)` – загружает промпт из файла
  - `inject_context(prompt, user_context)` – вставляет переменные (имя, дата и т.д.)
- [x] Обновить модули (таро, нумерология, астрология и др.) для использования промптов через `PromptManager`
- [x] Добавить кэширование промптов в памяти для скорости

**Ожидаемый результат:** Каждый модуль использует оптимизированный, контекстный промпт, улучшая качество ответов.

**Статус:** PromptManager полностью готов, 11 промптов загружены. Интеграция с модулями ожидает обновления хэндлеров (после решения конфликта getUpdates).

### 📈 Средний приоритет (после отладки)

#### 4. Полная интеграция UX‑пакета
**Цель:** Активировать все фичи UX – рейтинги, прогресс-бары, интерактивные кнопки.

**Действия:**
- [ ] Проверить работу `feedback_service.py` – сохранение оценок, расчёт среднего
- [ ] Настроить отправку прогресс-индикаторов в длительных операциях (через `progress.py`)
- [ ] Добавить inline‑кнопки рейтинга (👍/👎) в ответы AI‑модулей
- [ ] Реализовать команду `/stats` для показа статистики пользователя

**Ожидаемый результат:** Пользователь видит интерактивные элементы, может оценивать ответы, наблюдает прогресс.

#### 5. Восстановление админ‑бота
**Цель:** Запустить админ‑бота без конфликта с основным.

**Действия:**
- [ ] Проверить `admin_bot.py` на использование того же `BOT_TOKEN` (должен быть другой токен!)
- [ ] Настроить отдельный `.env.admin` с `ADMIN_BOT_TOKEN` и `ADMIN_USER_ID`
- [ ] Модифицировать `run_bots.py` для запуска обоих ботов с разными конфигами
- [ ] Убедиться, что оба бота могут работать параллельно (разные polling offset)

**Ожидаемый результат:** Админ‑бот работает, обрабатывает заказы, консультации, черновики.

### 🛠 Технический долг

#### 6. Рефакторинг `get_session_maker` в хэндлерах
**Цель:** Устранить все вызовы `get_session_maker()()` в пользу передачи `session_maker` через middleware.

**Действия:**
- [ ] Провести аудит всех хэндлеров (`admin.py`, `ai_mode.py`, других) – найти оставшиеся вызовы
- [ ] Заменить на получение `session_maker` из middleware (или использовать глобальный session maker)
- [ ] Убедиться, что нет падений при обработке запросов

**Ожидаемый результат:** Стабильная работа с БД, отсутствие `TypeError`.

#### 7. Настройка Redis для FSM
**Цель:** Восстановить Redis storage для состояний (сейчас используется memory storage).

**Действия:**
- [ ] Установить Redis локально или настроить подключение к облачному Redis
- [ ] Обновить `REDIS_URL` в `.env` (формат `redis://localhost:6379/0`)
- [ ] Перезапустить бота, убедиться, что FSM сохраняется между перезапусками

**Ожидаемый результат:** Состояния пользователей сохраняются при перезапуске бота.

---

## 🧪 Тестирование

### Тест‑кейсы
1. **Основной поток:** `/start` → `/menu` → выбор модуля → получение ответа → оценка 👍/👎
2. **Персонализация:** Несколько запросов в AI‑режиме – проверить, что ответы учитывают историю
3. **Модульные промпты:** Запуск каждого из 11 модулей – убедиться, что используется специализированный промпт
4. **Админ‑функции:** Создание заказа, подтверждение платежа, управление черновиками

### Критерии успеха
- [ ] Бот отвечает в течение 3‑5 секунд
- [ ] Все интерактивные кнопки работают (меню, рейтинги)
- [ ] Данные сохраняются в БД (профили, рейтинги, история)
- [ ] Админ‑бот видит новые заказы и может их обрабатывать

---

## 📁 Файлы для изменения

### Ключевые файлы
- `bot/main.py` – регистрация роутеров, middleware
- `bot/services/llm.py` – интеграция A1/A2
- `bot/services/prompt_manager.py` – доработка под 11 модулей
- `bot/services/profile_service.py` – **новый файл**
- `bot/handlers/ux_menu.py` – отладка обработки команд
- `bot/middlewares/auth.py` – проверка на блокировку updates
- `run_bots.py` – настройка запуска двух ботов
- `.env`, `.env.admin` – конфигурация токенов

### Вспомогательные
- `bot/prompts/` – 11 файлов промптов (готовы)
- `bot/models/user_profile.py` (готов)
- `bot/models/feedback.py` (готов)
- Все файлы UX‑пакета (готовы)

---

## 🕒 Оценка времени

| Задача | Оценка |
|--------|--------|
| 1. Отладка обработки команд | 1‑2 часа |
| 2. Интеграция A1 | 2‑3 часа |
| 3. Интеграция A2 | 2‑3 часа |
| 4. Полная интеграция UX | 3‑4 часа |
| 5. Восстановление админ‑бота | 1‑2 часа |
| 6. Рефакторинг session maker | 1‑2 часа |
| 7. Настройка Redis | 0.5‑1 час |

**Итого:** 10‑17 часов (2‑3 рабочих дня)

---

## 📝 Примечания

- **Токен GitHub** хранится в `github_token.txt`, не коммитить!
- **Perplexity Research** режим использовался для генерации UX‑пакета; можно продолжать использовать для деталей реализации.
- **Тестирование с пользователем** – Макс может проверить отклик бота в своём Telegram (ID 576704037).
- **Приоритет:** сначала отладка (задача 1), затем A1/A2, потом UX и админ.

---

## 🐛 Текущая диагностика (21:46)

### Состояние
- **Бот запущен** (PID 11880), polling активен.
- **Конфликт getUpdates устранён** – найден и убит скрытый процесс Python (PID 6052).
- **Updates обрабатываются** – лог показывает `Update id=384724840 is handled. Duration 6 ms`.
- **Ошибок в логах нет** (кроме предыдущих конфликтов).

### Проблема
Бот получает и обрабатывает команды `/start` и `/menu`, но **не ясно, отправляет ли он ответы пользователю**.

**Возможные причины:**
1. Хэндлер `cmd_menu` не отправляет сообщение из‑за исключения (не логируется).
2. Клавиатура `main_menu_kb()` содержит ошибку, из‑за которой `message.answer` падает.
3. Пользователь не видит ответы в Telegram Web (нужна проверка).

### Действия
1. **Проверить Telegram Web** – видит ли Макс ответы бота на `/menu`?
2. **Включить более детальное логирование** – добавить log в хэндлер `cmd_menu`.
3. **Временно упростить** – заменить клавиатуру на простый текст для теста.

---

**Следующий шаг:** Дождаться ответа Макса о видимости ответов бота, затем приступить к задаче 1.

*Документ обновлён: 2026‑02‑14 21:46*